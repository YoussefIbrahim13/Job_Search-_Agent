"""
Application services.

Composition layer between the API routes and the source/ranking packages. Route
handlers stay concerned with HTTP — validation, status codes, error mapping —
and the pipeline lives here.
"""
